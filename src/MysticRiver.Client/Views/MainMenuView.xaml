<UserControl x:Class="MysticRiver.Client.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="0,0,0,12" />
            <Setter Property="Padding" Value="18,12" />
            <Setter Property="MinWidth" Value="280" />
            <Setter Property="Background" Value="#FF2D3546" />
            <Setter Property="Foreground" Value="#FFF5F7FA" />
            <Setter Property="BorderBrush" Value="#FF8FA6D9" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="16" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF111827" Offset="0" />
                <GradientStop Color="#FF1F2937" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="460"
                Padding="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="#CC0F172A"
                BorderBrush="#AA9AAED6"
                BorderThickness="1"
                CornerRadius="12">
            <StackPanel>
                <TextBlock HorizontalAlignment="Center" FontSize="40" FontWeight="Bold" Foreground="#FFF8FAFC" Text="Mystic River" />
                <TextBlock Margin="0,8,0,24" HorizontalAlignment="Center" Foreground="#FFD1D5DB" Text="Choose a game mode" />
                <Button Style="{StaticResource MenuButtonStyle}" Content="Single Player" Click="SinglePlayerButton_Click" />
                <Button Style="{StaticResource MenuButtonStyle}" Content="Multiplayer (Coming Soon)" IsEnabled="False" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
